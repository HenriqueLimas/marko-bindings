import type { createStore, PrimitiveAtom } from "jotai/vanilla";

export interface Input {
  atom: PrimitiveAtom<number>;
  show: boolean;
  store: ReturnType<typeof createStore>;
}

<if=input.show>
  <use-atom/value=input.atom store=input.store/>
  <output>${value}</output>
</if>
