# @marko-bindings/tanstack-store

## 0.1.0

### Minor Changes

- 77bbaaa: Add render-wide client and store initializer tags so consumer bindings can omit repeated dependency getters while retaining explicit per-tag overrides.
- 2e353e8: Add the initial Marko 6 binding with resumable dependency getters, writable `<let-atom>` and reactive `<const-selected>` tags, including custom comparison for both bindings and optional identity selection, and re-export the TanStack Store API.

## 0.0.0

Initial development version.
