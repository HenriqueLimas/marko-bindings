import type { Atom } from "@tanstack/store";

export interface Input {
  atom: Atom<number>;
  compare?: (previous: number, next: number) => boolean;
}

<let-atom/value=() => input.atom compare=input.compare/>
<output>${value}</output>
<button onClick() {
  value += 5;
}>
  Add five
</button>
