import type { Atom } from "@tanstack/store";

export interface Input {
  atom: Atom<number>;
}

<use-atom/value=input.atom/>
<output>${value}</output>
<button onClick() {
  value += 5;
}>
  Add five
</button>
