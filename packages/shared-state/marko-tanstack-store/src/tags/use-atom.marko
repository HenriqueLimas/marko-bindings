import type { Atom } from "@tanstack/store";

export interface Input<T> {
  value: Atom<T>;
}

<const/atom=input.value>
<use-selector/selected=(value) => value store=atom/>
<return=selected
  valueChange(next) {
    atom.set(next);
  }
>
