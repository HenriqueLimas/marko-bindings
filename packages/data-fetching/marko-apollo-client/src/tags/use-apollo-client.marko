import type { ApolloClient } from "@apollo/client";

export interface Input {}

<let/client=$global.apolloClient as ApolloClient>
<script>
  queueMicrotask(() => {
    client = $global.apolloClient as ApolloClient;
  });
</script>

<return=client>
