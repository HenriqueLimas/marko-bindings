# @marko-bindings/apollo-client

## 0.1.0

### Minor Changes

- 77bbaaa: Add render-wide client and store initializer tags so consumer bindings can omit repeated dependency getters while retaining explicit per-tag overrides.
- 328c2b7: Add the initial Apollo Client binding with reactive `<await-query>`, `<const-fragment>`, `<const-mutation>`, and `<const-subscription>` tags and the complete `@apollo/client` public API re-exported from the package entrypoint.

## 0.0.0

Initial development version.
