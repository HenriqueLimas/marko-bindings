export interface Input {
  url: URL;
}

<ts-root-route/rootRoute>
  <@component|{ content }|>
    <${content}/>
  </@component>
</ts-root-route>

<ts-route/postRoute
  parent=rootRoute
  path="/posts/$postId"
  loader=({ params }) => ({ title: `Post ${params.postId}` })
>
  <@component|{ route }|>
    <h1>${route.loaderData.title}</h1>
    <output>${route.params.postId}</output>
  </@component>
</ts-route>

<ts-route-tree/routeTree root=rootRoute children=[postRoute]/>
<ts-router/router routeTree=routeTree url=input.url/>
<ts-router-provider router=router/>
