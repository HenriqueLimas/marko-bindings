---
"@marko-bindings/tanstack-form": minor
---

Make `field.state.value` a writable Marko binding so native inputs can use `value:=` and `checked:=`, while retaining `field.handleChange` as an imperative escape hatch.
