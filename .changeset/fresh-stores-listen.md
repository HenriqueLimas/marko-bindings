---
"marko-tanstack-store": minor
---

Add the initial Marko 6 binding with `<create-store>`, `<create-atom>`, writable `<use-atom>`, and reactive `<use-selector>` tags, and re-export the TanStack Store API.
