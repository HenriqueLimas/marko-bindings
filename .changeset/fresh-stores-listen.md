---
"marko-tanstack-store": minor
---

Add the initial Marko 6 binding with writable `<use-atom>` and reactive `<use-selector>` tags, including custom comparison for both bindings and optional identity selection, and re-export the TanStack Store API.
