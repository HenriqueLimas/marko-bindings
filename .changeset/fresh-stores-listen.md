---
"@marko-bindings/tanstack-store": minor
---

Add the initial Marko 6 binding with resumable dependency getters, writable `<let-atom>` and reactive `<const-selected>` tags, including custom comparison for both bindings and optional identity selection, and re-export the TanStack Store API.
