---
"marko-tanstack-query": minor
---

Add the initial TanStack Query binding with a resumable, reactive `<await-query>` tag and the complete `@tanstack/query-core` public API re-exported from the package entrypoint.
