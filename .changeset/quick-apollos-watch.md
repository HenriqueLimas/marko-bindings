---
"marko-apollo-client": minor
---

Add the initial Apollo Client binding with reactive `<use-query>`, `<use-fragment>`, `<use-mutation>`, and `<use-subscription>` tags and the complete `@apollo/client` public API re-exported from the package entrypoint.
