---
"@marko-bindings/tanstack-form": minor
---

Add typed field-array actions and `mode="array"` structural subscriptions to `<const-field>`, preserving indexed field metadata across inserts, removals, replacements, moves, swaps, and clears.
