import TaskPlanner from "../task-planner.marko";

<main>
  <section class="hero">
    <div class="eyebrow">Marko Run × Jotai</div>
    <h1>Keep the plan small.<br><em>Keep the state shared.</em></h1>
    <p>
      Writable and derived atoms drive one reactive Marko view. No provider and
      no tuple-shaped hooks—just tag variables backed by Jotai.
    </p>
  </section>

  <TaskPlanner/>
</main>

<style>
  main {
    display: grid;
    grid-template-columns: minmax(0, 0.85fr) minmax(30rem, 1.15fr);
    gap: clamp(2rem, 7vw, 8rem);
    align-items: center;
    width: min(74rem, calc(100% - 2rem));
    min-height: 100vh;
    margin: 0 auto;
    padding: 4rem 0;
  }

  .hero {
    max-width: 31rem;
  }

  .eyebrow,
  .label {
    color: #137d66;
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  h1 {
    margin: 1rem 0 1.5rem;
    color: #17352f;
    font-family: Georgia, "Times New Roman", serif;
    font-size: clamp(3rem, 6.5vw, 5.8rem);
    font-weight: 500;
    letter-spacing: -0.055em;
    line-height: 0.92;
  }

  h1 em {
    color: #d86438;
    font-weight: 500;
  }

  .hero p {
    max-width: 27rem;
    margin: 0;
    color: #66736f;
    font-size: 1.05rem;
    line-height: 1.75;
  }

  .planner {
    overflow: hidden;
    border: 1px solid rgb(23 53 47 / 10%);
    border-radius: 1.5rem;
    background: #fffdf8;
    box-shadow: 0 2rem 5rem rgb(77 66 45 / 14%);
  }

  .planner__header,
  .planner__footer,
  .toolbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }

  .planner__header {
    padding: 1.75rem 2rem 1.4rem;
  }

  h2 {
    margin: 0.35rem 0 0;
    color: #17352f;
    font-family: Georgia, "Times New Roman", serif;
    font-size: 1.8rem;
    font-weight: 500;
  }

  .progress {
    display: grid;
    justify-items: end;
    color: #7a847f;
    font-size: 0.72rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .progress strong {
    color: #137d66;
    font-size: 1.45rem;
    letter-spacing: -0.04em;
  }

  .add-task {
    display: flex;
    gap: 0.65rem;
    padding: 0 2rem 1.5rem;
  }

  .add-task input {
    min-width: 0;
    flex: 1;
    padding: 0.85rem 1rem;
    border: 1px solid #d9ddd7;
    border-radius: 0.75rem;
    color: #17352f;
    background: white;
  }

  .add-task input::placeholder {
    color: #9ba39f;
  }

  .add-task button {
    padding: 0.85rem 1.15rem;
    border: 0;
    border-radius: 0.75rem;
    color: white;
    background: #137d66;
    font-weight: 750;
  }

  .toolbar {
    padding: 0.85rem 2rem;
    border-block: 1px solid #ebece7;
    background: #f7f8f3;
  }

  .filters {
    display: flex;
    gap: 0.25rem;
  }

  .filters button,
  .reset,
  .remove {
    border: 0;
    color: #74807b;
    background: transparent;
    font-size: 0.8rem;
    font-weight: 700;
  }

  .filters button {
    padding: 0.45rem 0.65rem;
    border-radius: 0.5rem;
  }

  .filters button.active {
    color: #0d6b58;
    background: #dfeee8;
  }

  .remaining {
    color: #7a847f;
    font-size: 0.78rem;
  }

  .task-list {
    display: grid;
    min-height: 14rem;
  }

  .task {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr) auto;
    gap: 0.85rem;
    align-items: center;
    padding: 1rem 2rem;
    border-bottom: 1px solid #efefe9;
  }

  .task.done .task__title {
    color: #9aa29e;
    text-decoration: line-through;
  }

  .check {
    display: grid;
    width: 1.35rem;
    height: 1.35rem;
    padding: 0;
    place-items: center;
    border: 1.5px solid #aeb8b3;
    border-radius: 50%;
    color: white;
    background: white;
    font-size: 0.72rem;
    font-weight: 900;
  }

  .check[aria-pressed="true"] {
    border-color: #137d66;
    background: #137d66;
  }

  .task__title {
    overflow: hidden;
    color: #33443f;
    text-overflow: ellipsis;
  }

  .remove {
    opacity: 0;
  }

  .task:hover .remove,
  .remove:focus-visible {
    color: #bb4d2b;
    opacity: 1;
  }

  .empty {
    display: grid;
    gap: 0.35rem;
    place-content: center;
    min-height: 14rem;
    color: #9aa29e;
    text-align: center;
  }

  .empty strong {
    color: #61706a;
  }

  .planner__footer {
    padding: 1rem 2rem;
    background: #17352f;
  }

  .planner__footer p {
    margin: 0;
    color: #b8c8c2;
    font-size: 0.75rem;
  }

  .planner__footer code {
    color: #f0b59b;
    background: transparent;
  }

  .reset {
    flex: none;
    color: #f0b59b;
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    clip-path: inset(50%);
  }

  @media (max-width: 58rem) {
    main {
      grid-template-columns: 1fr;
      gap: 2.5rem;
      max-width: 40rem;
      padding: 3rem 0;
    }

    .hero {
      max-width: 36rem;
    }

    h1 {
      font-size: clamp(3.3rem, 11vw, 5rem);
    }
  }

  @media (max-width: 38rem) {
    main {
      width: min(100% - 1rem, 40rem);
      padding: 1.5rem 0;
    }

    .planner__header,
    .add-task,
    .toolbar,
    .task,
    .planner__footer {
      padding-inline: 1rem;
    }

    .toolbar,
    .planner__footer {
      align-items: flex-start;
      flex-direction: column;
    }

    .add-task {
      align-items: stretch;
      flex-direction: column;
    }

    .remove {
      opacity: 1;
    }
  }
</style>
