export interface Input {
  content: Marko.Body;
}

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta
      name="description"
      content="A task planner demonstrating Jotai atoms with native Marko bindings"
    >
    <title>Marko + Jotai task planner</title>
    <style>
      :root {
        color: #1f2933;
        background: #f6f3ea;
        font-family:
          Inter,
          ui-sans-serif,
          system-ui,
          -apple-system,
          BlinkMacSystemFont,
          "Segoe UI",
          sans-serif;
        font-synthesis: none;
      }

      * {
        box-sizing: border-box;
      }

      body {
        min-width: 20rem;
        margin: 0;
      }

      button,
      input {
        font: inherit;
      }

      button {
        cursor: pointer;
      }

      button:focus-visible,
      input:focus-visible {
        outline: 3px solid rgb(19 125 102 / 28%);
        outline-offset: 2px;
      }

      code {
        padding: 0.1rem 0.35rem;
        border-radius: 0.35rem;
        color: #0d6b58;
        background: #e6f3ee;
        font-size: 0.9em;
      }
    </style>
  </head>
  <body>
    <${input.content}/>
  </body>
</html>
