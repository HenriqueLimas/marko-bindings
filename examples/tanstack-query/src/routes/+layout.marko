export interface Input {
  content: Marko.Body;
}

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta
      name="description"
      content="TanStack Query data fetching with Marko Run and a local JSON API"
    >
    <title>Marko + TanStack Query</title>
    <style>
      :root {
        color: #19332d;
        background: #edf4ef;
        font-family:
          Inter,
          ui-sans-serif,
          system-ui,
          -apple-system,
          BlinkMacSystemFont,
          "Segoe UI",
          sans-serif;
        font-synthesis: none;
      }

      * {
        box-sizing: border-box;
      }

      body {
        min-width: 20rem;
        margin: 0;
      }

      button,
      a {
        font: inherit;
      }

      button:focus-visible,
      a:focus-visible {
        outline: 3px solid rgb(216 109 57 / 35%);
        outline-offset: 3px;
      }

      code {
        padding: 0.12rem 0.38rem;
        border-radius: 0.35rem;
        color: #8e3d1d;
        background: #f9e5d9;
        font-size: 0.9em;
      }
    </style>
  </head>
  <body>
    <${input.content}/>
  </body>
</html>
