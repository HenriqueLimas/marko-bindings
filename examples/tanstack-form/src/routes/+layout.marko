export interface Input {
  content: Marko.Body;
}

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta
      name="description"
      content="A resumable workshop registration built with Marko and TanStack Form"
    >
    <title>Marko + TanStack Form registration</title>
    <style>
      :root {
        color: #26302d;
        background: #f3f0e8;
        font-family:
          Inter,
          ui-sans-serif,
          system-ui,
          -apple-system,
          BlinkMacSystemFont,
          "Segoe UI",
          sans-serif;
        font-synthesis: none;
      }

      * {
        box-sizing: border-box;
      }

      body {
        min-width: 20rem;
        margin: 0;
      }

      button,
      input,
      select {
        font: inherit;
      }

      button {
        cursor: pointer;
      }

      button:focus-visible,
      input:focus-visible,
      select:focus-visible {
        outline: 3px solid rgb(197 83 52 / 24%);
        outline-offset: 2px;
      }

      code {
        padding: 0.1rem 0.35rem;
        border-radius: 0.3rem;
        color: #923b28;
        background: #f9ded4;
        font-size: 0.9em;
      }
    </style>
  </head>
  <body>
    <${input.content}/>
  </body>
</html>
