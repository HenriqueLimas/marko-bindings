export interface Input {
  content: Marko.Body;
}

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta
      name="description"
      content="A resumable shopping cart built with Marko and TanStack Store"
    >
    <title>Marko + TanStack Store cart</title>
    <style>
      :root {
        color: #212524;
        background: #eef1eb;
        font-family:
          Inter,
          ui-sans-serif,
          system-ui,
          -apple-system,
          BlinkMacSystemFont,
          "Segoe UI",
          sans-serif;
        font-synthesis: none;
      }

      * {
        box-sizing: border-box;
      }

      body {
        min-width: 20rem;
        margin: 0;
      }

      button {
        font: inherit;
        cursor: pointer;
      }

      button:focus-visible {
        outline: 3px solid rgb(189 84 37 / 30%);
        outline-offset: 2px;
      }

      code {
        padding: 0.1rem 0.35rem;
        border-radius: 0.3rem;
        color: #85401f;
        background: #f8e4d8;
        font-size: 0.9em;
      }
    </style>
  </head>
  <body>
    <${input.content}/>
  </body>
</html>
